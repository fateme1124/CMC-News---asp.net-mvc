@{
    ViewBag.Title = "وبسایت خبری";
}

@Html.Partial("_Slider")

@Html.Action("ShowNewsGroup", "Newses", new { locId = 1 })

<div class="main">
    <div class="row">
        <div class="col-md-4">
            @Html.Action("LastNews", "Newses", new { count = 4 })
            @Html.Action("BestNews", "Newses", new { count = 3 })
        </div>
        <div class="col-md-8">
            <div class="news-item2">
                @*@Html.Action("LastNews1", "Newses")*@
            </div>
        </div>
    </div>
</div>